# Ubiquiti WiFi Person Detector Configuration - EXAMPLE
# Define your access points and floors
# Replace the example values with your actual AP details.
# If you want to enable auto-discovery of Ubiquiti APs, set access_points to an empty list [] or remove the section.

access_points: []  # Empty list enables auto-discovery via ARP scan (requires local network access)
# To manually configure APs (recommended for setting floor/location), use the format below:
# access_points:
#   - name: "AP-Ground-Floor-Living"
#     mac: "aa:bb:cc:dd:ee:ff"
#     floor: "ground"
#     location: "living_room"
#     threshold: -70
#   - name: "AP-Ground-Floor-Kitchen"
#     mac: "11:22:33:44:55:66"
#     floor: "ground"
#     location: "kitchen"
#     threshold: -70

# Detection settings
detection:
  # Minimum number of APs that must detect a person to confirm presence
  min_aps: 2
  # Time window in seconds to consider for detection
  time_window: 10
  # Cooldown period after detection to avoid duplicate triggers (seconds)
  cooldown: 30

# Logging
logging:
  level: "INFO"
  file: "wifi_detector.log"
EOF